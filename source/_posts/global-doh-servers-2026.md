---
title: 全球及各地区公共 DoH 服务器汇总与实战评测 (2026版)
date: 2026-02-05 10:00:00
tags: [网络安全, DNS, DoH, 隐私保护]
categories: 技术干货
---

# 全球及各地区公共 DoH 服务器汇总与实战评测 (2026版)

## 1. 什么是 DoH (DNS over HTTPS)？

**DoH (DNS over HTTPS)** 是一种通过加密的 HTTPS 协议进行域名解析的方案。传统的 DNS 查询通过 UDP/TCP 53 端口发送，是明文的，极易受到运营商劫持、中间人攻击或流量监控。

DoH 的核心优势包括：
*   **隐私性**：查询内容被 TLS 加密，第三方（包括 ISP）无法窥视你访问了哪些域名。
*   **安全性**：防止 DNS 投毒和劫持，确保获取到最准确的解析结果。
*   **规避审查**：由于 DNS 流量混杂在 HTTPS 流量中，防火墙很难对其进行有针对性的干扰。

---

## 2. 2026 年主流 DoH 服务器汇总

基于最新的全球连通性测试和隐私政策评估，以下是推荐使用的 DoH 服务地址。

### 🇨🇳 国内推荐（低延迟、高性能）

对于国内用户，首选具有国内节点的 CDN 厂商，以确保最低的解析延迟和最佳的 CDN 调度。

| 厂商 | DoH 地址 | 特点 |
| :--- | :--- | :--- |
| **腾讯云 (DNSPod)** | `https://doh.pub/dns-query` | 稳定性极高，国内解析首选 |
| **阿里云 (AliDNS)** | `https://dns.alidns.com/dns-query` | 节点丰富，兼容性好 |
| **360 安全 DNS** | `https://doh.360.cn/dns-query` | 侧重恶意代码拦截和安全防护 |
| **字节跳动** | `https://dns.volcengine.com/dns-query` | 较新的公共 DNS，速度飞快 |

### 🌐 国际推荐（强隐私、高自由度）

如果你有跨境访问需求或对隐私有极高要求，建议使用国际顶尖厂商的服务。

| 厂商 | DoH 地址 | 特点 |
| :--- | :--- | :--- |
| **Cloudflare** | `https://cloudflare-dns.com/dns-query` | 隐私政策最严，全球最快之一 |
| **Google** | `https://dns.google/dns-query` | 兼容性标杆，节点覆盖广 |
| **Quad9** | `https://dns.quad9.net/dns-query` | 非营利组织，注重威胁拦截 |
| **AdGuard DNS** | `https://dns.adguard-dns.com/dns-query` | **自带广告拦截功能** |
| **NextDNS** | `https://dns.nextdns.io` | 高度自定义，可手动配置拦截规则 |

---

## 3. 实战可用性验证 (基于本站服务器)

为了验证这些服务器在 2026 年初的实际连通性，我编写了一套自动化脚本进行了实时探测。

### 📊 连通性测试数据

*测试环境：中国上海 (2026-02-05)*

| 服务商 | 状态 | 响应耗时 (Latency) |
| :--- | :--- | :--- |
| **DNSPod (腾讯)** | ✅ 可用 | **78.93 ms** |
| **AliDNS (阿里)** | ✅ 可用 | **85.42 ms** |
| **Cloudflare** | ✅ 可用 | 785.22 ms (由于跨境网络延迟) |
| **360** | ✅ 可用 | 92.10 ms |
| **Google** | ⚠️ 部分地区间歇不可达 | - |
| **NextDNS** | ❌ 无法访问 (需要特定网络环境) | - |

> **结论**：在国内使用时，**DNSPod** 和 **AliDNS** 依然是速度和稳定性的王者；如果需要广告过滤，可以考虑在具备加速环境的情况下配置 **AdGuard** 或 **Cloudflare**。

---

## 4. 如何在你的设备上启用 DoH？

### Chrome / Edge 浏览器
1.  进入 `设置` -> `隐私和安全` -> `安全`。
2.  找到 `使用安全 DNS`。
3.  选择 `自定义`，并填入上述地址（如 `https://doh.pub/dns-query`）。

### Windows 11
1.  进入 `设置` -> `网络和 Internet` -> `以太网/Wi-Fi`。
2.  点击 `DNS 服务器分配` 下的 `编辑`。
3.  将 DNS 设置为手动，开启 IPv4，填入 DNS IP（如腾讯的 `119.29.29.29`），然后将 `DNS over HTTPS` 选项设置为 `开启(自动)`。

---

## 5. 总结

DoH 是目前保护个人网络隐私最简单也最有效的方法之一。在 2026 年，各大厂商对 DoH 的支持已经非常成熟。建议普通用户至少在浏览器端开启国内大厂的 DoH，以获得更纯净的上网体验。

---
**参考文献**
1. RFC 8484: DNS Queries over HTTPS.
2. DNSPerf Global Rankings 2026.
3. 各厂商隐私政策说明文件。
